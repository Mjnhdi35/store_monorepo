name: API CI

on:
  push:
    branches: ['main']
    paths:
      - 'apps/api/**'
      - 'package.json'
      - 'pnpm-lock.yaml'
  pull_request:
    branches: ['main']
    paths:
      - 'apps/api/**'
      - 'package.json'
      - 'pnpm-lock.yaml'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate-api:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          run_install: false

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'pnpm'

      - name: Get pnpm store directory
        shell: bash
        run: |
          echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_ENV

      - name: Cache Node Modules
        id: cache-node-modules
        uses: actions/cache@v4
        with:
          path: |
            node_modules
            apps/api/node_modules
            ${{ env.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-${{ hashFiles('pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-

      # Skip install if cache hit, otherwise install
      - name: Install Dependencies
        if: steps.cache-node-modules.outputs.cache-hit != 'true'
        run: pnpm install --frozen-lockfile

      # Strict sequential steps: Fail fast if any step fails
      - name: Lint
        run: pnpm --filter @smoothie_store/api lint

      - name: Unit Tests
        run: pnpm --filter @smoothie_store/api test

      - name: Build
        run: pnpm --filter @smoothie_store/api build
